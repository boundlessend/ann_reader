name: test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: show swift version
        run: swift --version
      - name: run ANNKit tests
        run: swift test
